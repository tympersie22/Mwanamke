# Synthetic idempotency value in payment injection test; not a credential.
# Exact rule/file/line fingerprint; any line move or other finding fails closed.
/src/apps/api/src/app.test.ts:generic-api-key:42
